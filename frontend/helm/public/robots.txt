# Tropis — crawler directives.
# Public marketing/content routes are crawlable; the logged-in app shell is not.
# Adjust the Disallow list as you add real public pages.

User-agent: *
Allow: /

# Private app areas — no SEO value, keep them out of the index.
Disallow: /users
Disallow: /state-demo

# Point crawlers at the sitemap. Replace the host with your production origin
# (or generate this file at build time from VITE_SITE_URL).
Sitemap: https://example.com/sitemap.xml
