{
  "name": "@tropis/desktop",
  "private": true,
  "version": "0.0.0",
  "description": "Tauri v2 desktop shell wrapping the built frontend (apps/frontend/helm/dist). `build` deliberately passes --no-bundle so the repo-wide `pnpm -r build` does not depend on per-OS installer tooling (the macOS DMG bundler fails on many dev machines); run `bundle` to produce installers.",
  "scripts": {
    "tauri": "tauri",
    "build": "tauri build --no-bundle",
    "bundle": "tauri build",
    "build:debug": "tauri build --debug --no-bundle",
    "check": "cd src-tauri && cargo check"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  },
  "engines": {
    "node": ">=22"
  }
}
