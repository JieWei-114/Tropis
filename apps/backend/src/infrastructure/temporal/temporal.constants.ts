export const TEMPORAL_CLIENT = 'TEMPORAL_CLIENT';
export const TEMPORAL_TASK_QUEUE = 'main';

/** Workflow type name — must match the exported function in workflows.ts. */
export const ONBOARDING_WORKFLOW = 'userOnboardingWorkflow';
