{
  "//": [
    "Lighthouse CI config — scores all five audit categories on the built SPA.",
    "Assertions are 'warn' (visible in the PR, non-blocking) on purpose: tune the",
    "thresholds to your real numbers first, then flip the ones you want to enforce",
    "to 'error' so regressions fail CI. Run locally: `npx @lhci/cli autorun`."
  ],
  "ci": {
    "collect": {
      "staticDistDir": "./dist",
      "numberOfRuns": 1
    },
    "assert": {
      "assertions": {
        "categories:performance": ["warn", { "minScore": 0.8 }],
        "categories:accessibility": ["warn", { "minScore": 0.9 }],
        "categories:best-practices": ["warn", { "minScore": 0.9 }],
        "categories:seo": ["warn", { "minScore": 0.9 }]
      }
    },
    "upload": {
      "target": "temporary-public-storage"
    }
  }
}
