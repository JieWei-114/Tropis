apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Thin wrapper so `kubectl kustomize infra/k8s` builds the base manifests.
# Environment overlays (e.g. overlays/prod) reference ../../base directly.
resources:
  - base
